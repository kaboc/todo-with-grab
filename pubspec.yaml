name: todo_with_grab
description: "A todo app using Grab."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  equatable: ^2.0.7
  grab: ^1.0.2
  hive_ce: ^2.16.0
  hive_ce_flutter: ^2.3.3
  meta: ^1.17.0
  path_provider: ^2.1.5
  pot: ^0.9.0

dev_dependencies:
  flutter_lints: ^6.0.0
  path: ^1.9.1
  test: ^1.28.0

flutter:
  uses-material-design: true
